import React from 'react';

const AddChapterForm: React.FC = () => {
  return (
    <div>
      <h2>Add Chapter (Admin)</h2>
      {/* This component is a placeholder for future development. */}
    </div>
  );
};

export default AddChapterForm;
