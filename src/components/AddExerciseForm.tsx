import React from 'react';

const AddExerciseForm: React.FC = () => {
  return (
    <div>
      <h2>Add Exercise (Admin)</h2>
      {/* This component is a placeholder for future development. */}
    </div>
  );
};

export default AddExerciseForm;
