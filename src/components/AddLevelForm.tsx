import React from 'react';

const AddLevelForm: React.FC = () => {
  return (
    <div>
      <h2>Add Level (Admin)</h2>
      {/* This component is a placeholder for future development. */}
    </div>
  );
};

export default AddLevelForm;
