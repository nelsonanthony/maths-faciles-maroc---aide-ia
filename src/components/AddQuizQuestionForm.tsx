import React from 'react';

const AddQuizQuestionForm: React.FC = () => {
  return (
    <div>
      <h2>Add Quiz Question (Admin)</h2>
      {/* This component is a placeholder for future development. */}
    </div>
  );
};

export default AddQuizQuestionForm;
