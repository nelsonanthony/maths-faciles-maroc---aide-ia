// Ce fichier centralise la configuration de l'application.

/**
 * L'URL de base pour toutes les requêtes API.
 * - Pour une utilisation en ligne (Vercel), utilisez un chemin relatif comme '/api'.
 * - Pour une utilisation avec un serveur local, changez cette valeur pour l'adresse IP et le port de votre serveur.
 *   Exemple : 'http://192.168.1.100:8000/api'
 */
export const API_BASE_URL = '/api';
